
let initialState = {
}

const toggleUserMenuReducer = (state = initialState, action:any) => {
    switch (action.type){
        default:
            return state
    }

}


export default toggleUserMenuReducer;
